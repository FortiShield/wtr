# .branchopsconfig - Shared repository configuration for branchops
# This file should be committed to the repository root.

[copy]
    # Files to copy from the main repository to each new worktree
    include = .env.example
    include = .nvmrc
    include = .ruby-version
    
    # Directories to copy (use with caution for large folders)
    # includeDirs = node_modules
    # includeDirs = .venv
    
    # Exclude specific patterns within included directories
    # excludeDirs = node_modules/.cache

[hooks]
    # Commands to run in the worktree after creation
    # Relative paths are from worktree root
    postCreate = "./.branchops/hooks/post-create.sh"
    
    # Command to run before removing a worktree
    # If this fails (returns non-zero), removal is aborted
    preRemove = "./.branchops/hooks/pre-remove.sh"
    
    # Command to run after worktree is removed
    # postRemove = "echo 'Worktree removed'"

[defaults]
    # Default editor for 'new -e' or 'editor' commands
    # editor = vscode
    
    # Default AI tool for 'new -a' or 'ai' commands
    # ai = claude
