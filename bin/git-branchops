#!/usr/bin/env bash
# git-branchops - Git branchops runner (git subcommand wrapper)
# Allows running as: git branchops <command>

# Find this script's real location (resolve symlinks)
SRC="${BASH_SOURCE[0]}"
while [ -h "$SRC" ]; do
  DIR="$(cd -P "$(dirname "$SRC")" && pwd)"
  SRC="$(readlink "$SRC")"
  [[ $SRC != /* ]] && SRC="$DIR/$SRC"
done
SCRIPT_DIR="$(cd -P "$(dirname "$SRC")" && pwd)"

# Execute the main branchops script
exec "$SCRIPT_DIR/branchops" "$@"